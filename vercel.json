{
  "buildCommand": "npm run build --workspace=apps/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install --workspaces --include-workspace-root --legacy-peer-deps",
  "framework": "nextjs"
}
